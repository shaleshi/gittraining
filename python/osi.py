#Operating System Interface
import os#operating system
import shutil#copy
import glob#search for file
#The re module provides regular expression tools for advanced string processing.
import re
import math #math functions
import urllib2 #iInternet Access
#Two of the simplest are urllib2 for retrieving data from urls and smtplib for sending mail:
import datetime
import zlib #data compression
from timeit import Timer #Performance measurement
import doctest #quality control
import unittest #quality control
import pprint
t = [[[['black', 'cyan'], 'white', ['green', 'red']], [['magenta','yellow'], 'blue']]]
pprint.pprint(t)
import textwrap
import locale
from  string import Template
import struct
#The struct module provides pack() and unpack() functions for working with variable length binary record formats.
import thread ,zipfile #Threading is a technique for decoupling tasks which are not sequentially dependent
import logging #The logging module offers a full featured and flexible logging system.
#At its simplest, log messages are sent to a file or to sys.stderr
import weakref, gc
#Python does automatic memory management
#(reference countinig for most objects and garbage collection to eliminate cycles).
from array import array
from collections import deque
import bisect
from heapq import heapify, heappop, heappush
from decimal import *
import random
#python module installation: python setup.py install
